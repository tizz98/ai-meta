# GENERATED by ai-meta (meta init/upgrade). Edit meta.toml, not this file.
name: meta check
on:
  push:
    branches: [main]
  pull_request:

jobs:
  standards:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
{{#if self_hosting}}      - uses: dtolnay/rust-toolchain@stable
{{/if}}      - name: Enforce codified standards
        run: {{meta_cmd}} check --strict
