---
name: meta-test
description: Run the project's tests.
---

# meta test

GENERATED by ai-meta (`meta init`/`meta upgrade`). Do not edit; change
`.meta/meta.toml` and run `./meta upgrade`.

Run the test command, or the coverage command with --coverage.

## Usage

```
./meta test [--coverage]
```

Run `./meta test --help` for the full option list.
