---
name: meta-stats
description: Repo analytics — commits & lines of code.
---

# meta stats

GENERATED by ai-meta (`meta init`/`meta upgrade`). Do not edit; change
`.meta/meta.toml` and run `./meta upgrade`.

Show commit counts by author (`commits [--user]`) or lines of code by language (`cloc [--lang]`); every subcommand supports --json.

## Usage

```
./meta stats <commits|cloc> [--json]
```

Run `./meta stats --help` for the full option list.
