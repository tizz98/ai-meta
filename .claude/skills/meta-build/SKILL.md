---
name: meta-build
description: Build the project.
---

# meta build

GENERATED by ai-meta (`meta init`/`meta upgrade`). Do not edit; change
`.meta/meta.toml` and run `./meta upgrade`.

Run the profile/config build command (auto-runs codegen first).

## Usage

```
./meta build
```

Run `./meta build --help` for the full option list.
