---
name: meta-setup
description: Bootstrap GitHub structure.
---

# meta setup

GENERATED by ai-meta (`meta init`/`meta upgrade`). Do not edit; change
`.meta/meta.toml` and run `./meta upgrade`.

Idempotently create labels, milestones, and the project board.

## Usage

```
./meta setup [--dry-run]
```

Run `./meta setup --help` for the full option list.
