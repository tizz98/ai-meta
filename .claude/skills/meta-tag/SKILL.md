---
name: meta-tag
description: Cut a release.
---

# meta tag

GENERATED by ai-meta (`meta init`/`meta upgrade`). Do not edit; change
`.meta/meta.toml` and run `./meta upgrade`.

Bump the version across configured locations, commit, tag, and push.

## Usage

```
./meta tag [major|minor|patch|vX.Y.Z]
```

Run `./meta tag --help` for the full option list.
