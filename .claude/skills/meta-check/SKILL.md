---
name: meta-check
description: Enforce codified standards.
---

# meta check

GENERATED by ai-meta (`meta init`/`meta upgrade`). Do not edit; change
`.meta/meta.toml` and run `./meta upgrade`.

Run the configured guards; CI runs with --strict.

## Usage

```
./meta check [--strict] [--json]
```

Run `./meta check --help` for the full option list.
