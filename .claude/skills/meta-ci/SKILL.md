---
name: meta-ci
description: Local merge gate.
---

# meta ci

GENERATED by ai-meta (`meta init`/`meta upgrade`). Do not edit; change
`.meta/meta.toml` and run `./meta upgrade`.

Run all gates mirroring CI and post a collapsed PR comment.

## Usage

```
./meta ci [PR]
```

Run `./meta ci --help` for the full option list.
