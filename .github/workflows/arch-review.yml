# GENERATED by ai-meta (meta init/upgrade). Edit meta.toml, not this file.
name: architecture review
on:
  pull_request:

jobs:
  architecture-review:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@stable
      - name: Architecture review (advisory)
        # Advisory: ticket-candidate signals exit 2 but must not fail the job.
        run: cargo run --quiet -- arch || true
        # Marker for the upsert comment: <!-- ai-meta-arch-review -->
